query projectId($login: String!, $maxProjectV2EntriesPerPage: Int!) {
  organization(login: $login) {
    projectsV2(
      first: $maxProjectV2EntriesPerPage
      orderBy: { field: NUMBER, direction: ASC }
    ) {
      nodes {
        id
        number
      }
    }
  }
}
